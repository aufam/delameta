image: gcc

stages:
  - build
  - test

build:
  stage: build
  before_script:
    - apt update && apt -y install cmake
  script:
    - mkdir -p build
    - cmake -B build -DDELAMETA_BUILD_APP=ON -DDELAMETA_BUILD_TEST=ON -DCMAKE_BUILD_TYPE=Debug
    - cmake --build build -j10 -t build-deb
  artifacts:
    paths:
      - build/test/test_all
      - build/delameta-1.0.0.deb

test:
  stage: test
  script:
    - ./build/test/test_all

